#[flake8]
#exclude = E128,E722,W605,E127,E501
#max-line-length = 150

# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = qrmagic/_version.py
versionfile_build = qrmagic/_version.py
tag_prefix = qrmagic-v
parentdir_prefix = NVTK-
